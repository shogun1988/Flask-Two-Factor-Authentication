# Copy this file to `.env` and adjust values as needed.
# python-decouple will read from environment variables or a .env file at project root.

# Flask config object path
APP_SETTINGS=config.DevelopmentConfig

# Database connection string
# For local dev, SQLite is fine; replace with your Postgres URL in production
DATABASE_URL=sqlite:///dev.sqlite

# Security
SECRET_KEY=change-me

# App display name used in 2FA provisioning URI
APP_NAME=Flask2FA
